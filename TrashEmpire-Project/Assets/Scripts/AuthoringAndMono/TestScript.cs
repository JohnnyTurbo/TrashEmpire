using System;
using DotsNav.Hybrid;
using DotsNav.PathFinding.Hybrid;
using UnityEngine;

namespace TMG.TrashEmpire
{
    public class TestScript : MonoBehaviour
    {
        public DotsNavAgent _DotsNavAgent;
        public DotsNavPathFindingAgent dna;
        
        private void Update()
        {
            //dna.FindPath(new Vector3(10,0,0));
        }
    }
}
